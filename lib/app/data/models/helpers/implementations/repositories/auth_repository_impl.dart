import '../../../user.dart';
import 'repository_impl.dart';

abstract class AuthRepositoryImpl extends RepositoryImpl {
  // Future<Map<String, dynamic>> register(User user);

  // Future<Map<String, dynamic>> login(User user);

  // Future<User> update(User user);

  // Future<bool> delete(int id);

  // Future<User> find(int id);

  // Future<List<User>> findAllFromUnit(int unitId);

  // Future<List<User>> findSliceFromUnit(int unitId, {int from, int to});

  // Future<List<User>> findAll();

  // Future<List<User>> findSlice({int from, int to});
}
